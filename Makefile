# develop:
# 	npx webpack serve

install:
	npm ci

# start-backend:
# 	npm start

# build:
# 	rm -rf dist
# 	NODE_ENV=production npx webpack

# publish:
# 	npm publish --dry-run

# lint:
# 	npx eslint .
